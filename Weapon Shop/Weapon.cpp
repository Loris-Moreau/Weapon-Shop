#include "Weapon.h"

Weapon::Weapon(string wName, string wDescription, string wType, int wWeight, int wDamages, int wPrice, float wDurability)
	:Name(wName), Description(wDescription), WeaponType(wType), Weight(wWeight), Damages(wDamages), Price(wPrice), Durability(wDurability)
{

}
